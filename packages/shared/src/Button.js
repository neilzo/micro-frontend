import React from 'react';
import PropTypes from 'prop-types';

const Button = props => {
  return (
    <button>
      i shared button
    </button>
  );
};

Button.propTypes = {

};

export default Button;