{
  "name": "my_micro_fe",
  "private": true,
  "version": "1.0.0",
  "description": "Example of a micro frontend architecture",
  "main": "index.js",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "dev:clients": "lerna run dev:client --stream",
    "dev:servers": "lerna run dev:server --stream",
    "start": "lerna run",
    "build": "lerna run build",
    "lint:all": "lerna run lint --stream",
    "format:all": "lerna run format --stream",
    "test:all": "CI=true lerna run test --stream"
  },
  "workspaces": [
    "packages/*"
  ],
  "keywords": [
    "frontend",
    "react",
    "microservices",
    "micro",
    "frontend",
    "monorepo"
  ],
  "author": "Neil Daftary",
  "license": "MIT",
  "dependencies": {
    "lerna": "^3.20.2"
  }
}
